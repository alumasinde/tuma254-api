meta {
  name: Protected Route
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/api/v1/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

tests {
  test("authenticated access token reaches protected route", function() {
    expect(res.status).to.equal(200);
  });
}
