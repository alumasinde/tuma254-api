meta {
  name: Require Customer
  type: http
  seq: 5
}

get {
  url: {{BASE_URL}}/api/v1/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

tests {
  test("authenticated customer can access me", function() {
    expect(res.status).to.equal(200);
  });
}
