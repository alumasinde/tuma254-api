meta {
  name: Forbidden Role
  type: http
  seq: 6
}

get {
  url: {{BASE_URL}}/api/v1/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

tests {
  test("authorization foundation protects authenticated routes", function() {
    expect(res.status).to.be.oneOf([200, 403]);
  });
}
