# Bruno API Testing

Commit collections and safe example environments.

Never commit production access tokens, database credentials, M-Pesa secrets, JWT secrets, or real production passwords.

Recommended local environment variables:
- BASE_URL=http://localhost:8080
- ACCESS_TOKEN=

Keep real production environments outside Git. Use production.bru.example only as a template.
