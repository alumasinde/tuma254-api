services:
  mongodb:
    image: mongo:8
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: tuma254_dev
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
