meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  body: none
  auth: none
}

tests {
  test("health succeeds", function() {
    expect(res.status).to.equal(200);
  });
}
